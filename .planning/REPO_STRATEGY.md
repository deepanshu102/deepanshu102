# Repository Strategy: deepanshu102

## 1. Pinned Repositories (Target State: 6)

| Slot | Repository | Primary Stack | Purpose |
|:---:|:---|:---|:---|
| 1 | `BlockChain-go` | Golang | Existing highlight — demonstrates Go + Distributed logic. |
| 2 | `microservices-demo` | Golang / k8s | **[NEW]** Orchestration patterns, service discovery, and circuit breakers. |
| 3 | `grpc-go-patterns` | Golang / gRPC | **[NEW]** Protobuf definitions, streaming RPCs, and interceptors. |
| 4 | `aws-lambda-dynamo` | Golang / AWS | **[NEW]** Event-driven serverless architecture in Go. |
| 5 | `deepanshu102.github.io` | JS / CSS / HTML | Portfolio source code — showcases frontend/Three.js skills. |
| 6 | `system-design-notes` | Markdown | **[NEW]** Architectural documentation and decision logs. |

---

## 2. Topic Tags Matrix

All active/pinned repositories must have these topics for SEO and professional signal:

- **Core Tags:** `golang`, `backend`, `distributed-systems`, `system-architecture`, `cloud-native`.
- **Specific Tags:**
  - `BlockChain-go`: `blockchain`, `p2p`, `distributed-ledger`.
  - `microservices-demo`: `kubernetes`, `docker`, `microservices`, `resilience`.
  - `grpc-go-patterns`: `grpc`, `protocol-buffers`, `api-design`.
  - `aws-lambda-dynamo`: `aws-lambda`, `dynamodb`, `serverless`, `aws-sdk`.

---

## 3. Archival Logic (Stats Optimization)

To achieve a clean "Golang dominant" Top Languages card, the following repo categories should be archived (which removes them from stats cards but keeps them on your profile):

- **Coursework:** `BCA-projects-*`, `MCA-assignments-*`, `College-Lab-*`.
- **Legacy University:** Any repos using `C`, `C++`, or `Python` that are older than 2021.
- **Tutorials:** Repos like `java-tutorial`, `python-basics`, `git-crash-course`.
- **Dormant:** Projects with no updates since your graduation/start of professional career (2020).

---

## 4. README Standard (Per Pinned Repo)

Each of the 6 pinned repos must implement this structure:
1. **Title + Status Badge**
2. **Abstract** (1-2 sentences)
3. **Internal Architecture** (ASCII/Mermaid diagram)
4. **Implementation Highlights** (What specific hard problem it solves)
5. **Stack Table**
6. **Local Deployment** (HUD-style terminal commands)
